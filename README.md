# Agency HQ — מרכז הבקרה של בית התוכנה

מערכת-על לניהול בית התוכנה/הייעוץ שלך. לא מערכת ללקוח — אלא המערכת שמנהלת **אותך**: את כל הלקוחות, את המערכות שאתה מפתח לכל אחד מהם, את התזרים שלך, ואת מודלי החיוב.

הבנויה על אותו stack ענן מוכח: **Cloudflare Pages** (אירוח) + **D1** (SQLite) + **Hono** (API) + **Drizzle** (ORM). עברית מלאה RTL.

הזרימה: אתר סטטי (`/public`) → API (`/functions/api`) → D1.

## מה יש במערכת

| מסך | מה הוא עושה |
|-----|-------------|
| **לוח בקרה** | התמונה הכוללת: MRR, ARR, צנרת, לקוחות פעילים, מערכות חיות, תמהיל הכנסה לפי מודל חיוב |
| **לקוחות** | כל עסק שאתה משרת — כרטיס 360 עם מערכות, התקשרויות, מרכזי רווח, בריאות לקוח ומסע הלקוח |
| **מערכות** | כל מערכת מידע שאתה מפתח: סטטוס, התקדמות %, stack, כתובת חיה, repo |
| **התקשרויות** | מודל החיוב מול כל לקוח — מה שמזין את ה-MRR והתזרים |
| **מחשבון חיוב** | ⭐ כל המשתנים במקום אחד. משווה 6 מודלי חיוב חיים (חד-פעמי / ריטיינר / שעתי / אחוז ממחזור / מבוסס-ערך / היברידי), מחשב רווח, שוליים, תעריף אפקטיבי לשעה ונקודת החזר. שומר תרחישים ויוצר התקשרות בלחיצה |
| **תזרים** | תחזית חודשית (6/12/24 ח') המשלבת הכנסות אוטומטיות מההתקשרויות הפעילות + תנועות ידניות (הוצאות, כלים, קבלני משנה), עם יתרת פתיחה ומאזן מצטבר |
| **מרכזי רווח** | רעיונות עסקיים והכנסות חדשות — עם פוטנציאל חודשי, סבירות וערך משוקלל |
| **תהליכים** | תהליכי עבודה, מסעות לקוח ונהלים (SOP) שאתה בונה — עם שלבים ואחראים |
| **מתודולוגיה** | ⭐ ספריית פורמטים מקצועיים לכל מסע המוצר (אפיון → הצעה → פיתוח → QA → אבטחה/פרטיות → עלייה לאוויר → מסירה → תחזוקה → ייעוץ). כל פורמט ניתן לעריכה, ואפשר "להחיל" אותו על לקוח/מערכת כדי שיהפוך למהלך חי עם צ'ק־ליסט ומעקב התקדמות. כולל תבניות מסמך מוכנות למילוי (חוזה פיתוח, DPA, מדיניות פרטיות). ראה `docs/METHODOLOGY.md` |
| **בריאות מסירה** | מטריצה של כל לקוח מול שערי מסע המוצר (אפיון/הצעה/פיתוח/QA/אבטחה/עלייה/מסירה): ✓ עבר · % בעבודה · ＋ לפתיחה. תמונת מוכנות במבט אחד |
| **משימות** | לוח קנבן: לביצוע / בעבודה / הושלם |

> **קישור אוטומטי:** שינוי שלב במסע של לקוח פותח אוטומטית את המהלך המתאים במתודולוגיה (ליד→שיחת גילוי, בבנייה→הגדרת "גמור", וכו׳) — בלי כפילויות.

## האתר הציבורי (ort-tech.co.il)

אתר שיווקי רב-עמודי שמוביל כל מבקר לפעולה (השארת פרטים / וואטסאפ / חיוג). כל העמודים חולקים
מערכת עיצוב אחת (`public/site.css`) ו-Header/Footer מוזרקים (`public/site.js`).

| כתובת | קובץ | תיאור | `source` של הליד |
|-------|------|-------|------------------|
| `/` | `public/index.html` | **עמוד הבית** — תמומנת-על + הכוונה לכל העמודים. כולל חותמת "עסק של מילואימניק" | `website-home` |
| `/services` | `public/services.html` | שירותים — 3 עמודי התווך, "איך זה עובד", "מה זה לא" | — |
| `/for-whom` | `public/for-whom.html` | למי זה מתאים — כאבים, סקטורים, מרכזי רווח | — |
| `/about` | `public/about.html` | אודות — הסיפור האישי + חותמת המילואימניק | — |
| `/contact` | `public/contact.html` | צור קשר — טופס מלא + טלפון/וואטסאפ/מייל | `website-contact` |
| `/login` | `public/login.html` | **כניסת לקוחות** — פורטל למערכות החיות (ONE · צימרים · ניהול העסק) | — |
| `/app` | `public/app.html` | **הדשבורד הפנימי** (Agency HQ — ניהול העסק). מוגן ב-PIN | — |
| `/lp` | `public/lp.html` | דף נחיתה חלופי (כהה, טכני) | `landing` |
| `/lp2` → `/` | — | הפניה 301 לעמוד הבית (קישורים ישנים) | — |

> **הגדרות מרוכזות:** כל פרטי הקשר (טלפון, וואטסאפ, מייל) וכתובות הכניסה למערכות מרוכזים
> באובייקט `SITE` בראש `public/site.js` — נקודת עריכה אחת.
>
> **קישור הדומיין** `ort-tech.co.il` לפרויקט ה-Pages מתבצע בלוח הבקרה של Cloudflare
> (Workers & Pages → agency-hq → Custom domains), לא בקוד. הפריסה עצמה רצה מ-`main` בלבד (`.github/workflows/deploy.yml`).

כל הטפסים מזינים את אותו webhook לידים (`/api/hook/lead`) עם `source` שונה לכל עמוד, כך שבדשבורד
רואים בפאנל **לידים לפי מקור** מאיפה הגיע כל ליד.

## מודל הנתונים (D1 / Drizzle)

`clients` · `systems` · `engagements` · `scenarios` · `cashflow` · `profit_centers` · `processes` · `playbooks` · `playbook_runs` · `tasks` · `settings` · `admin_sessions`

הסכמה המלאה: `src/db/schema.ts`. המיגרציות: `migrations/`.

### לב החישוב — מודלי חיוב

הערך החודשי (MRR) של כל התקשרות מחושב לפי מודל החיוב שלה (`src/api/engagements.ts` → `engagementMonthly`):

- **ריטיינר / מבוסס-ערך** → הסכום החודשי הקבוע
- **שעתי** → תעריף שעה × שעות בחודש
- **אחוז ממחזור** → מחזור הלקוח × האחוז
- **חד-פעמי** → 0 חודשי (רק מקדמת הקמה)
- **היברידי** → סכום כל הרכיבים

תחזית התזרים (`src/api/finance.ts` → `/finance/forecast`) משלבת את כל ההתקשרויות הפעילות עם התנועות הידניות למאזן מצטבר חודשי.

## הקמה חד-פעמית (Cloudflare)

1. פותחים חשבון חינמי ב-Cloudflare.
2. יוצרים בסיס נתונים D1 ומעתיקים את ה-`database_id` ל-`wrangler.toml`:
   ```bash
   npx wrangler d1 create agency-hq-db
   ```
3. יוצרים פרויקט Pages ומחברים אותו ל-repo הזה ב-GitHub (Build command ריק, Output directory: `public`).
4. מוסיפים binding ל-D1 בשם `DB` בהגדרות ה-Pages.
5. מריצים את המיגרציות: `npm run db:migrate:remote`.

בכניסה הראשונה המערכת תבקש להגדיר **קוד גישה** (4 ספרות ומעלה) — הוא נשמר מגובב (SHA-256).

## פיתוח מקומי

```bash
npm install
npm run db:migrate:local      # יוצר טבלאות ב-D1 מקומי
npm run dev                    # http://localhost:8790
```

## עדכונים

כל דחיפה ל-`main` ב-GitHub מריצה את `.github/workflows/deploy.yml`: בדיקת טיפוסים → מיגרציות על ה-D1 החי → פריסה ל-Cloudflare Pages. המיגרציות רצות תמיד לפני הפריסה, כדי שקוד חדש לא יפגוש סכמה ישנה.

### הגדרה חד-פעמית

הצינור דורש secret בשם `CLOUDFLARE_API_TOKEN` תחת **Settings → Secrets and variables → Actions**, עם הרשאות `D1:Edit` ו-`Cloudflare Pages:Edit`. (`CLOUDFLARE_ACCOUNT_ID` כבר מוגדר בקובץ ואינו סוד.)

בלי הטוקן ה-workflow **נכשל במפורש** בשלב הראשון. זו התנהגות מכוונת: בגרסה קודמת הוא דילג בשקט וסיים ירוק, כך שמיזוגים נראו כאילו עלו לאוויר בזמן ששום דבר לא נפרס ושום מיגרציה לא רצה.

### פריסה ידנית

```bash
npx wrangler d1 migrations apply agency-hq-db --remote   # תמיד קודם
npx wrangler pages deploy public --project-name agency-hq --branch main
```

### מעקב המיגרציות (רקע)

עד 16/08/2026 לא הייתה בבסיס הנתונים החי טבלת `d1_migrations` — המיגרציות הוחלו ידנית,
ומנגנון המעקב של wrangler לא ידע על אף אחת מהן. במצב הזה `migrations apply` היה מנסה
להריץ מחדש את `0001` על טבלאות קיימות ונופל, כלומר הצינור לא היה יכול לפרוס בכלל.

הטבלה נוצרה והוזנה בכל 15 המיגרציות שכבר הוחלו, כך שמעכשיו הצינור מריץ רק מיגרציות
חדשות. **אין להחיל מיגרציה ידנית בלי לרשום אותה בטבלה** — אחרת הפער הזה נפתח מחדש:

```sql
INSERT OR IGNORE INTO d1_migrations (name) VALUES ('00NN_xxx.sql');
```
